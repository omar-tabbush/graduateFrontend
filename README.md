# graduateProject CV builder website

### frontend using : { react - react-pdf - styled-components - pure css } (as a primary frontend)
### backend using :{nestjs - postgresql - (orm: not desided yet)}
